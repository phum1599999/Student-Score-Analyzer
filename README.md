# Student Score Analyzer (Python)

Mini project สำหรับฝึกเขียนโปรแกรมภาษา Python พื้นฐาน  
โดยเน้นการใช้ฟังก์ชัน (Function), ลูป (Loop) และลิสต์ (List)

---

## 📌 Features

- รับคะแนนนักเรียน (ช่วง 0–100)
- กด Enter เพื่อหยุดการรับคะแนน
- แสดงคะแนนทั้งหมด
- คำนวณคะแนนสูงสุด
- คำนวณคะแนนต่ำสุด
- คำนวณคะแนนเฉลี่ย
- นับจำนวนนักเรียนที่ผ่าน (คะแนน ≥ 50)
- ระบบเมนูใช้งานผ่าน Command Line

---

## 🧾 Program Usage

1. เมื่อเริ่มโปรแกรม ระบบจะให้ผู้ใช้ป้อนคะแนนนักเรียนทีละค่า  
   (คะแนนต้องอยู่ในช่วง 0–100)

2. ผู้ใช้สามารถป้อนคะแนนได้หลายครั้งตามต้องการ  
   เมื่อป้อนคะแนนครบแล้ว ให้กด Enter โดยไม่ต้องพิมพ์ค่าใด ๆ  
   เพื่อเข้าสู่เมนูหลักของโปรแกรม

3. หลังจากเข้าสู่เมนูหลัก โปรแกรมจะแสดงตัวเลือกดังนี้

   1. แสดงคะแนนทั้งหมด
   2. แสดงคะแนนสูงสุด
   3. แสดงคะแนนต่ำสุด
   4. แสดงคะแนนเฉลี่ย
   5. แสดงจำนวนนักเรียนที่ผ่าน (คะแนน ≥ 50)
   6. ออกจากโปรแกรม

4. ผู้ใช้เลือกเมนูโดยพิมพ์หมายเลขเมนูที่ต้องการ  
   โปรแกรมจะแสดงผลลัพธ์ตามเมนูที่เลือก

5. หากเลือกเมนูที่ต้องใช้ข้อมูลคะแนน แต่ยังไม่มีการป้อนคะแนน  
   โปรแกรมจะแจ้งเตือนให้ผู้ใช้ป้อนคะแนนก่อน

6. เมื่อเลือกเมนู 0 โปรแกรมจะสิ้นสุดการทำงาน

---

## 🗂 Project Structure
